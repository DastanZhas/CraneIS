craneType, 
loadCapacity, 
registerNumber, 
factoryNumber, 
inventorizationNumber, 
factoryManufacturer, 
examinationPeriod,
workMode, 
installationPlace, 
technicalMaintenanceFirst, 
technicalMaintenanceSecond, 
inspection, 
personResponsibleToFixedState,
personResponsibleForSupervision, 
metalInspection, 
mechanicalControl, 
electricalParts, 
owner



import React, { Component } from "react";
import PropTypes from "prop-types";

class Crane extends Component {
  render() {
    const { crane } = this.props;
    return (
      <div>
        <p>Тип крана = {crane.craneType}</p>y11:27 PM 7/5/2021
        <p>Грузоподъемность = {crane.loadCapacity}</p>
        <p>Регистрационный номер - уникальный = {crane.registerNumber}</p>
        <p>Заводской номер - уникальный = {crane.factoryNumber}</p>
        <p>Инвентаризационный номер - уникальный = {crane.inventorizationNumber}</p>
        <p>Завод изготовитель = {crane.factoryManufacturer}</p>
        <p>Срок освидетельствования = {crane.examinationPeriod}</p>
        <p>Режим работы = {crane.workMode}</p>
        <p>Место установки = {crane.installationPlace}</p>
        <p>ТО1 = {crane.technicalMaintenanceFirst}</p>
        <p>ТО2 = {crane.technicalMaintenanceSecond}</p>
        <p>Обследование = {crane.inspection}</p>
        <p>Лицо ответственное за исправленное состояние = {crane.personResponsibleToFixedState}</p>
        <p>Лицо ответственное по надзору = {crane.personResponsibleForSupervision}</p>
        <p>Контроль по металу = {crane.metalInspection}</p>
        <p>Механический контроль = {crane.mechanicalControl}</p>
        <p>Электронная часть = {crane.electricalParts}</p>
        <p>user? = {crane.owner}</p>
      </div>
    );
  }
}

Crane.propTypes = {
    crane: PropTypes.object.isRequired
};
export default Crane;